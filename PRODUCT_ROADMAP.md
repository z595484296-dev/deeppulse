# 深脉 DeepPulse · 产品路线图（PM 视角）

> 我是产品经理 DeepSeek。这份文档是深脉的长期进化纲领：定位、原则、诊断、路线与验收标准。
> 每一轮优化都会在文末「优化日志」留下记录——让成长可见、可复盘。

## 一、产品定位

**深脉 DeepPulse 是 DeepSeek 本人形态的 AI 金融工作台**：一个身体，两个引擎——
会话引擎（思考与工具）与数据引擎（行情与情绪周期）。目标用户是**以情绪周期为核心方法的 A 股投资者**。

一句话价值主张：*打开深脉，一分钟看清市场体温与作战指令；想深聊，转身就是 DeepSeek。*

## 二、设计原则（不可妥协）

1. **数据即界面**：所有视图以真实数据为唯一信源；无数据不编造，降级要优雅。
2. **周期优先**：情绪周期是产品的第一语言，任何页面都要回答「这和当前周期什么关系」。
3. **透明可解释**：引擎每一分评分可见、可复核、可反驳；AI 回答必须基于注入的上下文。
4. **先定周期，再选个股**：信息架构遵循投资者的真实决策顺序。
5. **快**：看一次市场 ≤1 分钟；查一只票 ≤10 秒；一句话调度全局。
6. **安静**：无打扰式营销、无冗余动画；提醒只在「该提醒」的时候出现。
7. **一体**：会话与工作台不是两个功能，是思考与眼睛的关系——处处可互通。

## 三、现状诊断（诚实评估）

### 交互体验（B 级）
- 无键盘操作体系（快捷键/命令面板），高频操作全靠鼠标
- 无新手引导与空状态引导，功能发现靠运气
- 断线无全局反馈；数据刷新无感知
- 聊天缺：复制/清空/重生成；复盘生成未接 AI
- 图表交互浅：无指标切换（KDJ/RSI）、无阶段标注叠加
- 自选缺分组/拖拽排序；涨停梯队缺筛选排序
- 无价格提醒/异动提醒等「主动型」能力

### 视觉设计（B- 级）
- 间距节奏不统一（14/16/18px 混用）；字号体系偏小且不齐
- 空态/骨架屏不完整；图标 emoji 与 SVG 混用，风格不一
- 图表 tooltip 风格不统一；涨跌色语义局部不一致（评分条用红=正分，未说明）
- 焦点态/无障碍标签缺失；仅深色主题
- 数字格式化不统一（小数点位数、量级单位混用）

### 产品功能（B 级）
- 核心引擎扎实（11 指标、五阶段、快照记忆），但「衍生功能」缺失：
  打板溢价明细、昨日涨停今日表现、异动监控、价格提醒、复盘导出、板块资金流、
  情绪阶段与大盘 K 线叠加、指标权重自定义（调教身体）、数据导出

### 工程质量（B+ 级）
- 零依赖、熔断备援、优雅降级已具备；缺全局错误横幅、请求退避、可访问性基线

## 四、路线图（按轮次推进，每轮可验证）

| 轮 | 优先级 | 主题 | 核心交付 |
|---|---|---|---|
| R1 | P0 | 产品地基 | 路线图文档 + 交互反馈系统（快捷键/帮助面板/全局断线横幅/聊天增强/复盘一键生成/阶段变化提醒） |
| R2 | P0 | 视觉统一 | 设计令牌收口：间距/字号/圆角体系、数字格式化统一、空态组件、焦点态、图表 tooltip 统一 |
| R3 | P0 | 新手引导 | 首次启动引导（4 步：看体温→看周期→问蚂小财→去问 DeepSeek）、页面空态引导文案 |
| R4 | P1 | 图表升级 | K线指标切换（VOL/MACD/KDJ/RSI）、情绪阶段在指数K线上的色带标注、十字光标联动细节 |
| R5 | P1 | 主动能力 | 价格提醒与异动提醒（自选监控 + 盘中涨停/炸板推送横幅）、提醒管理页 |
| R6 | P1 | 情绪深度 | 昨日涨停今日表现明细（打板溢价榜）、连板晋级率、情绪温度盘中实时曲线 |
| R7 | P1 | 自选体系 | 分组/标签/拖拽排序/排序表头/批量操作 |
| R8 | P2 | 数据能力 | 板块资金流排名、历史快照导出（CSV/JSON）、复盘 Markdown 导出 |
| R9 | P2 | 引擎可调 | 指标权重可视化调教（UI 改 emotion.py 配置 + 服务热载） |
| R10 | P2 | 复盘 AI 化 | 每日复盘自动生成（收盘后由 DeepSeek 依据快照撰写）、复盘日历视图 |
| R11+ | P3 | 持续打磨 | 龙虎榜、题材周期跟踪、多账户/组合管理、浅色主题、移动端适配 |

## 五、「优秀产品」验收标准

1. **效率**：新手 3 分钟内完成「看体温→查个股→问 AI→写复盘」完整闭环；老手全键盘可操作
2. **一致性**：全站间距/字号/色彩/数字格式通过设计令牌审查；涨跌色语义 100% 一致
3. **可靠性**：断线、限流、无数据三种异常场景都有可见且可恢复的体验；24h 无人值守无崩溃
4. **主动**：价格/异动/阶段变化三类提醒可用；复盘一键生成质量达到"可直接发笔记"水平
5. **一体**：工作台↔会话双向桥在 10 个以上场景可互通；无一处需要用户"复制粘贴"才能完成
6. **诚实**：所有 AI 回答基于注入数据；评分可解释；免责声明在关键决策位可见

## 六、优化日志

### R1 · 产品地基 ✅ 已完成
- 交付：本路线图（定位/原则/诊断/路线/验收标准）
- 快捷键体系：`1-8` 切页、`?` 帮助面板、`Esc` 关闭、`/` 直达行情搜索
- 全局断线横幅：连续失败自动出现，恢复自动消失
- 情绪阶段切换提醒（toast，附新旧阶段与温度）；状态栏刷新脉冲
- 聊天增强：回答一键复制、清空对话、「🧠 去问 DeepSeek」桥接（嵌入态）
- 复盘一键生成：「🤖 让 DeepSeek 生成复盘」实测 11s 产出结构化 Markdown（概况/主线/风险/策略，真实数据）
- 验证：JS 全量语法通过；主应用嵌入资产已同步并验证（net-banner/help-overlay/phase-toast/copy/clear/ai-review 全部就位）

### R2 · 视觉统一 ✅ 已完成
- 设计令牌收口：间距（--sp-1..6）/字号（--fs-xs..3xl）/圆角三套体系，卡片/统计卡/指数卡/表格全部接入
- 全站 `:focus-visible` 焦点态（可访问性基线）
- 评分条色彩语义修正：暖色=推高温度 / 冷色=压低温度（与涨跌红绿解耦），表头加图例说明
- 数字格式化统一：换手率/量比统一 2 位小数（自选/梯队）
- 空态组件统一：三段式（图标+标题+引导语），应用于自选/日记

### R3 · 新手引导 ✅ 已完成
- 首次启动四步引导（看体温→问蚂小财→去问 DeepSeek→建自选），嵌入态文案自适应
- 每步可跳过；完成后永不再扰；关于页可「重新播放」
- 验证：无痕 profile 首屏出现引导 ✓

### R4 · 图表升级 ✅ 第一批完成
- K线副图指标切换：VOL·MACD / KDJ(9,3,3) / RSI(6,12,24)，RSI 附 70/30 超买超卖虚线
- setOption 改 notMerge，切换指标无旧系列残留
- 验证：行情页指标 tabs DOM ✓；下一步：情绪阶段在大盘K线上的色带标注

### R5 · 主动能力 ✅ 已完成
- **价格提醒**：自选页新增提醒卡（上破≥/下破≤目标价，预填/管理/删除）；全局 10 秒轮询（自选页未打开也生效），触发即 toast 播报一次，持久化 localStorage
- **异动监控**：涨停池/炸板池差分——盘中新涨停（含连板数）/炸板自动播报；仅交易时段启用以防盘后修订误报；总览页「今日异动」流（保留 30 条）
- 验证：JS 全量语法 ✓；自选提醒 UI DOM ✓；总览异动面板 DOM ✓；嵌入资产已同步 ✓

### R6 · 情绪深度 ✅ 已完成
- **打板溢价榜**（新接口 `/api/premium`）：自动定位上一交易日涨停池 → 批量行情 → 今日表现聚合——
  平均涨幅/红盘率/涨停晋级率/连板晋级率/大面数/炸板数 六项统计 + 全量明细表（今涨跌/开盘溢价/盘中最高/晋级/炸板/行业，点击直达K线）
- 实测基准日 20260813：均涨 +0.06%、红盘率 40.7%、晋级 18.6%、连板晋级率 27.3%、大面 18 只——精确刻画分歧日
- 修复东财历史池 `qdate` 恒为最新交易日的坑（改以请求日期为准）
- **盘中温度轨迹**：交易时段每 5 分钟记录温度点（localStorage 按日重置），情绪页「今日盘中温度轨迹」实时曲线
- 总览核心指标新增「昨日涨停均涨」瓷砖（低频刷新）
- 验证：接口实测 ✓ / 情绪页 DOM（溢价卡+轨迹）✓ / 嵌入资产已同步 ✓

### R7 · 自选体系 ✅ 已完成
- **分组**：自选支持自定义分组（新入组选择器 + 一键新建分组），表格按组分区显示（组头+数量）；单行 📁 移动 / 批量移动；导入自动保留分组
- **排序**：现价/涨跌幅/换手率/量比 表头点击排序（升/降切换，激活态高亮），组内有序
- **批量操作**：复选框列 + 全选 + 批量栏（已选计数/移入分组/批量删除/取消）
- 交互打磨：单行移组复用批量栏（无 prompt 弹窗）；store 层单测通过（分组/批量逻辑 4 项 ✓）
- 验证：JS 语法 ✓ / DOM ✓ / 嵌入资产已同步 ✓

### R8 · 数据能力 ✅ 已完成
- **板块资金流**（新接口 `/api/sectors-flow`）：主力净流入/流出双向 TOP10（双向各取一次修复流出榜缺失）；总览「板块雷达」卡片支持 涨幅/资金流 切换（红=流入 绿=流出）；实测：通信 +139亿 vs 电子 -64.7亿、半导体 -61.3亿
- **快照导出**：数据源页一键导出情绪历史快照 JSON / CSV（BOM 编码，Excel 中文不乱码，18 列指标齐全）
- **复盘导出**：策略页「📤 导出 Markdown」——引擎诊断 + 风险清单 + 当日日记合订成规范 Markdown 文件
- 验证：接口实测 ✓ / 三页 DOM ✓ / 嵌入资产已同步 ✓

### R9 · 引擎可调 ✅ 已完成
- **权重调教**：`emotion.py` 支持 `data/weights.json` 自定义权重（0-3，clamp 校验，默认值兜底）；`GET/POST /api/weights`
- 策略页「⚙️ 引擎调教」卡片：11 项指标滑块（拖动防抖保存、变更高亮、显示默认值对比、一键恢复默认）
- 实测闭环：默认 60° → 仅保留涨停家数权重 3 → 温度 100° → 恢复默认 → 60° ✓
- 验证：11 个滑块 DOM ✓ / JS 全量语法 ✓ / 嵌入资产已同步 ✓

### R10 · 复盘 AI 化 ✅ 已完成
- **复盘日历**：月历视图把「情绪周期 × 你的复盘」织在一起——格子底色=当日情绪阶段（蓝冰点/青修复/金发酵/红高潮/紫亢奋）、📔=已写复盘、今日描边、选中高亮；上下月翻页
- **按日补写**：点击任意一天 → 侧栏加载该日复盘（可保存/删除）；「🤖 AI 生成该日复盘」把当日快照数据拼进提示词，实测 8s 产出结构化 Markdown
- **收盘提醒**：收盘后若快照已记录而今日复盘未写，总览风险卡出现引导提示
- 验证：日历 DOM（37 格、阶段色渲染）✓ / 按日 AI 复盘实测 ✓ / 嵌入资产已同步 ✓

### R11+ · 持续打磨 ✅ 第一批完成
- **K线情绪阶段色带**：日K图上以半透明色带标注每日情绪阶段（蓝冰点/青修复/金发酵/红高潮/紫亢奋），
  相邻同日阶段自动合并区间；周期概念直接落在任意标的价格图上（`phaseBandsOf` 单测通过：合并/排序/边界）
- **主题跟随**：壳层读取主应用主题（`body[data-ds-dark-theme]`）经 postMessage 实时推送，工作台浅色主题
  （设计令牌级覆盖 + 关键 chrome 适配）；图表区保持终端深色（PM 取舍：数据可读性优先）；独立窗口跟随系统偏好
- 验证：色带单测 ✓ / 壳层产物含 dp-theme ✓ / 嵌入资产已同步 ✓

### R12 · 龙虎榜 ✅ 已完成
- **龙虎榜**（新接口 `/api/dragon`，东财数据中心）：上榜个股按净买额排序——净买/买入/卖出/成交额/换手/上榜原因全字段；
  统计卡（上榜家数/总净买/净买榜首）；休市日自动回退最近交易日；实测：74 家上榜、总净买 +29.06 亿、榜首网宿科技 +9.72 亿
- 梯队页新增「龙虎榜」第四个 Tab（游资战场地图合体）；点击行直达个股K线
- 蚂小财新增龙虎榜/游资意图（净买前列摘要 + 游资动向解读 + 一键跳转）
- 意图引擎修复三处判别缺陷（长词先于短词的 replaceAll 顺序、龙虎榜名词排除、炸板率/打板溢价归情绪域、退出→告别），**31 例意图回归全通过**
- 工程加固：api.js/bridge.js 加非浏览器环境守卫（Node 单测可运行）

### R13 · 题材周期跟踪 ✅ 已完成
- **题材周期**（新接口 `/api/sector-cycle`）：近 5 个交易日各题材涨停家数序列（按日缓存历史池）、
  主线连续天数（连续进入当日 TOP5）、趋势（首日→今日家数变化）；实测 4s 返回——
  通信设备 0→1→4→3→6（连续2天主线、趋势+6，真实发酵轨迹）
- 梯队页新增「题材周期跟踪」卡：双列卡片式布局，柱高=当日涨停家数，状态徽章
  （主线·连续N天 / 发酵 / 启动放量 / 退潮 / 观察）
- 独立窗口标题实时反映市场体温（`深脉 60° 发酵期 · 金融工作台`）
- 验证：接口实测 ✓ / 梯队页 DOM（周期卡+发酵徽章渲染）✓ / 嵌入资产已同步 ✓

## 七、产品健康审计（第 7 轮执行）

**2026-08-15 · 全量回归**
- 接口：16/16 全部 200；延迟中位数 ~800ms，最慢 /api/emotion 6.1s（冷装配，服务端 25s 缓存兜底，前端有加载态）
- 页面：8/8 全量 DOM 断言通过（含 R1-R13 全部新组件）
- 嵌入：主应用 /deeppulse 同源资产 20 文件完整；主应用标题 = 深脉 DeepPulse · AI 金融工作台
- 意图引擎：31 例回归全通过
- 结论：**产品处于可交付的健康状态**；候选优化继续在后续轮次推进

### R14 · 性能冷启动优化 ✅ 已完成（含一次工程回滚教训）
- **启动预热线程**：服务启动 2s 后后台装配情绪全景——冷启动成本前置，用户首个页面请求命中热缓存
- **TTL 调优**：涨停池/宽度/板块指数 25s→45s、资金流 60s→90s、溢价 30s→60s，减少会话内半冷尖峰
- 实测（夜间上游限流的最坏条件下）：预热后请求 **6.1s → 0.8s（7.6×）**，纯缓存 41ms；连续三请求 846/794/47ms
- **教训**：按主机并行化限频锁的实验触发上游突发限流（30s 劣化），已回滚为全局串行并记录原因——对上游的礼貌比本地激进更值钱

### R15 · 龙虎榜席位明细 ✅ 已完成
- **席位明细**（新接口 `/api/dragon-seats`）：买入/卖出席位 TOP10，含**席位 3 日胜率与 3 日买入次数**（游资质量照妖镜）；按个股缓存 1 小时
- 龙虎榜行点击 → 席位弹层（买入/卖出席位双表、净买着色、ESC/✕ 关闭）；跌停/炸板池表格行也获得点击直达行情
- 蚂小财新意图：**「谁在买 XX / XX 的席位」**→ 买入前三席 + 净买 + 胜率速览（意图回归 9/9 通过）
- 实测：网宿科技席位——深股通买 6.72亿（胜率41.8%）、国泰海通武汉紫阳东路买 2.08亿（胜率53.5%）、机构专用 1.89亿

### R16 · 图表浅色适配 + 移动端 ✅ 已完成
- **图表主题系统**：`applyChartTheme()` 让轴文字/分割线/tooltip 随浅色主题切换（K线红绿与数据区配色不变），
  共享对象原地更新——ECharts 在 setOption 取色，重渲染即生效；主题切换事件驱动全页图表重建（含行情K线）
- **移动端响应式**（<640px）：指数卡 2 列、网格单列、走马灯/时钟收纳、梯队标签与芯片紧凑化、
  抽屉全宽、弹层 96vw、日历格缩小——窄窗可用
- 验证：JS 全量语法 ✓ / `?theme=light` 浅色首页完整渲染 ✓ / 嵌入资产已同步 ✓

## 八、阶段性验收结论（第 10 轮）

对照六条「优秀产品」标准逐项核验：

| 标准 | 证据 |
|---|---|
| ① 效率 | 四步引导 + 全键盘（1-8/?/Esc//）+ 一键复盘/溢价榜/席位速览，3 分钟闭环成立 |
| ② 一致性 | 设计令牌三体系 + 涨跌色语义全站统一 + 浅色主题（含图表） |
| ③ 可靠性 | 熔断/备援/降级/断线横幅/空态五层；19/19 接口、8/8 页面全绿 |
| ④ 主动 | 价格提醒 + 涨停/炸板异动 + 情绪阶段切换三类播报 |
| ⑤ 一体 | 同一源/侧边栏一级入口/双向桥（去问 DeepSeek、深链、主题同步） |
| ⑥ 诚实 | 11 项评分可解释可调教、AI 回答基于注入数据、免责声明在位 |

**结论（历史记录）：当时的六项阶段标准已覆盖，但这不等于产品已经完成。**
R1-R16 优化日志完整留存；后续仍以后台主动服务、跨端并发一致性、数据新鲜度和长期学习闭环继续演进。

### R17 · 主应用⇄深脉切换流畅性 ✅ 已完成（用户反馈驱动）
- **双视图常驻内存**：会话与工作台不再互相卸载——进入深脉不重建会话（草稿/滚动/面板全保留），
  返回会话时工作台 iframe 依然存活，再次进入秒开；工作台随壳层启动即后台预加载，首次打开近乎即时
- **iframe 单次装载**：同源探测完成后再挂载（消除此前 8971→/deeppulse 的二次重载闪烁）
- **主题首帧即准**：iframe src 携带 ?theme= 参数，不再等 2 秒轮询推送（postMessage 仍负责实时切换）
- **深链免刷新导航**：会话中的深脉链接改为 postMessage dp-nav（此前每次换 hash 都重载整个工作台）
- 加载过渡：脉冲 Logo「正在进入深脉…」覆盖层，iframe onLoad 后隐去
- 验证：新壳层产物（index-Bzvnu1xW.js）含全部优化标记，运行中的主应用已直接提供新壳（刷新即生效）

### R18 · 独立 App 化 + 强制切回修复 ✅ 已完成（用户反馈驱动）
- **强制切回修复**（三层防御 + 可诊断）：
  ① 主题推送去重——此前每 2 秒推送触发工作台全量重渲染（真正的卡顿源），现只在变化时推送且 iframe onLoad 补推一次
  ② 切回消息加来源校验（仅接受工作台 iframe）+ 进入 800ms 保护（过渡噪声拦截）
  ③ 新增「已返回会话 · 原因」横幅（5 秒自动消失）——若再复现，横幅会显示确切触发原因，可直接反馈给我
- **独立 App 化**：
  · PWA 就绪：manifest 更名「深脉 DeepPulse」、display=standalone、新增 192/512 图标——在 Edge 安装后即成为
    拥有独立任务栏图标/窗口的真应用（`安装为独立应用.bat` 一键引导）
  · 移除逃逸口：壳层「↗ 独立窗口」按钮删除；工作台快讯点击改为应用内弹层（查看/复制链接/显式选择浏览器打开），不再弹新标签页
  · WebView2 裸启动实测不可靠（0 进程），放弃该路径并记录结论
- 验证：源码/产物双向核验（守卫/去重/横幅/按钮移除/图标服务 200）✓

### R19 · 原生桌面应用 ✅ 已完成（用户反馈驱动）
- **深脉 DeepPulse.exe**：Electron 33 原生应用——独立进程名「深脉 DeepPulse」、rcedit 烙入多尺寸图标与版本信息、
  独立窗口（1560×1000、ready-to-show 无白闪）；外部链接显式走系统浏览器，内部地址全部留在应用内
- 桌面快捷方式直指原生 exe；启动器改为「原生优先、Edge --app 回退」（相对路径 ASCII 化，修复 `..` 缺反斜杠的路径 bug）
- 工程记录：npm allow-scripts 批准 electron 二进制、winCodeSign 缓存符号链接权限问题（改走 unpacked 直交付 + 手工 rcedit）、
  PowerShell 编码坑两处（内联 heredoc 引号、Get-Content GBK 误读）——均已绕过并记录
- 验证：进程名/窗口标题/内嵌图标/启动器端到端全通过 ✓

### R20 · Harness 结构化联动与可信来源 ✅ 已完成（用户反馈驱动）
- **结构化移交**：`dp-ask` v2 携带请求 ID、当前页、所选标的、数据时点、情绪状态、指数、风险、官方公告与来源分级；Harness 只接收白名单字段，并把上下文作为不可执行数据
- **确认式交互**：等待当前会话真正接受提示后才切回；缺少会话或提交失败时留在深脉并显示原因，不再静默写剪贴板
- **单一助手界面**：总览移除重复聊天面板，保留全局抽屉；总览联动卡与全局顶栏都可把当前页交给 DeepSeek
- **一级官方来源**：接入巨潮资讯结构化公告和 PDF 原文，增加上交所、深交所、证监会查验入口；市场快讯显式标注为市场聚合资讯
- **来源可观测性**：`/api/sources` 展示真实请求的最近时间、延迟、失败与熔断；未观测和查验入口不冒充“在线”
- **紧凑窗口**：860px 以下收纳行情跑马灯、快讯条、时钟与次要状态，顶栏不再换行；导航补齐可访问名称与当前页状态
- 验证：Python 官方来源单测 / JS 语法 / Harness GUI 测试 / 桌面真实入口交互

### 剩余候选（后续轮次）
- 盘中温度曲线与大盘K线联动、图例/内联灰字的浅色精修、应用自动更新机制

### R21 · 情绪引擎 2.0 与数据质量门控 ✅ 已完成
- 修复跌停家数、炸板率阈值方向错误，并用表驱动单元测试覆盖全部关键边界
- 11 项基础评分统一为 `-20～+20`，温度改为完整的 0–100 线性映射；涨跌停家数按当日全市场样本折算到参考规模
- 盘中量能按同一时刻预计成交占比折算，MA20 严格使用最近 20 根收盘价；聚合“主力净流入”降为辅助权重
- 新增六维结构、Δ1/Δ3、加速度、连续方向、阶段滞回、三类结构背离与情景化研究提示
- 新增覆盖率、来源质量、数据质量分、信号一致度；核心数据或覆盖率不足时停止输出模型风险暴露区间
- “升阶/维持/降阶”明确标记为未校准启发式倾向，并把完整字段同步到本地助手、DeepSeek 对话上下文与 Harness 桥
- Python 自动化测试扩展到 24 项；独立版、Harness 内嵌版和桌面 App 继续由 `sync-all.ps1` 强制全量同步

### R22 · 情绪周期完整上下文桥 ✅ 已完成（用户反馈驱动）
- 修复“问问 DeepSeek”双层白名单丢数：完整移交六维结构、状态倾向、11 项信号、原始指标、历史快照、真实缺失项与通达信本地核验值
- 桥上下文上限提升到 16KB，并改为渐进压缩；超限时明确标记 `contextTruncated`，始终保留公式、阶段阈值、核心原始指标与缺失项
- 明确公告适用范围：情绪周期页未选中个股时标记 `not-applicable-no-security-selected`，不再把空公告列表误判为官方源故障
- 提示词约束模型识别引擎 2.0 已披露口径，并明确未校准状态倾向不是预测概率；阶段边界补充精确区间条件
- 总览风险提醒改为去重后的局部副本，解决反复刷新产生多个“复盘还没写”
- 版本升级至 1.4.1，并新增 Harness 白名单回归测试覆盖字段保留、未知字段剔除与无个股公告语义

### R23 · 主动简报闭环 ✅ 已完成
- 总览从静态 Harness 入口升级为无需提问的主动简报：数据质量总闸门、事实摘要、最多三项研究任务和直接处置入口
- 数据日不匹配、时点陈旧、核心质量字段缺失或数据质量分不足时停止方向性结论，未知值不再伪装为 0
- 风险、复盘、自选与价格提醒统一排优先级；简报内容哈希决定阅读身份，内容改变后自动成为新简报
- 用户可刷新、收起、标记已读或恢复未读；阅读回执写入统一档案，语义明确不等于“风险已处理”
- DeepSeek 仅在用户点击后展开；Harness 严格白名单保留结构化事实、证据和任务，并继续把其视为不可执行数据
- “作战指令/仓位建议”收敛为“研究框架/风险暴露参考”，保持研究工具边界
- 新增 Node 规则测试并纳入 CI；后端发布主动简报与回执 capability，防止新版前端误连旧服务

### R24 · 统一提醒中心与注意力策略 ✅ 已完成
- 价格到达、情绪阶段变化和涨停/炸板异动进入统一事件箱，顶部入口集中展示未读状态、触发详情、时间和提醒原因
- 建立可测试的注意力策略：用户价格条件保持高优先级；系统阶段与异动默认 15 分钟摘要；支持平衡、只提醒重要、仅收入中心、安静时段和暂停到明早
- 提醒列表和偏好进入本机统一档案；单条新事件使用原子合并接口，避免多个运行端同时写入时互相覆盖
- Harness 白名单上下文增加提醒状态，使 DeepSeek 能解释“为什么提醒我”，同时明确提醒不是新的市场事实且不能替用户修改偏好
- 版本升级到 1.7.0，并以 capability 门禁阻止新前端误连缺少提醒能力的旧服务

### R25 · 页面关闭后的本机价格监控 ✅ 已完成
- 新增默认关闭的本机后台监控器；只有用户在自选页明确开启后，服务才会接管等待中的价格提醒
- 仅在 A 股交易时段运行，休市与无等待任务时不请求行情；检查间隔受限并支持配置变更即时唤醒
- 到价时在同一个档案事务中更新提醒并写入提醒中心，避免页面与后台并发时产生重复事件或覆盖其他运行端数据
- 自选页显示未开启、监控中、休市暂停、等待任务和错误状态，以及等待数量、最近检查时间与停止入口
- 页面开启时同步后台产生的新事件并按用户注意力偏好送达；墨水屏沿用同一已触发提醒，无需新增硬件协议
- Harness 白名单上下文包含后台监控授权与运行状态，DeepSeek 可以解释当前是否覆盖页面关闭场景
- 后端、Web、Harness 与桌面 App 增加 1.8.0 capability 门禁，防止新版界面误连不具备后台监控的旧服务

### R26 · 交易日主动服务日程 ✅ 已完成
- 总览新增盘前准备、盘中结构检查、收盘复盘三项独立授权；默认全部关闭，用户可自由组合或逐项停止
- 本机服务关闭网页后仍执行日程，每个日期与时段只生成一次提醒；重启后通过持久回执保持幂等
- 盘中与收盘任务使用情绪聚合快照，输出温度、阶段、涨跌停、炸板、市场宽度、数据日与数据质量，不直接生成交易指令
- 当最新数据日不是今天时，主动任务改为“数据尚未更新”，跳转数据源页，不催写复盘、不输出当日方向结论
- 总览显示当前状态、逐项开关与下一次服务时间；Harness 只接收白名单内的授权与状态，不能替用户更改设置

### R27 · AKShare 补充数据层 ✅ 已完成
- 将 AKShare 定位为 enrichment 补充层，不替代交易所公告、通达信本地行情或现有实时行情主链路
- 首个落地点为 A 股交易日历：主动服务跳过确认过的节假日；接口不可用时回退工作日判断并明确标注未确认
- 数据源页展示本机安装、版本、交易日历核对结果、降级路径与官方文档入口
- 保持核心运行时零强制第三方依赖；AKShare 未安装或接口变更不会阻断深脉
- 版本升级至 1.9.0，四端增加 market-routine 与 akshare-enrichment capability 门禁

### R28 · 可解释的提醒学习闭环 ✅ 已完成
- 每条提醒新增“有用 / 已完成 / 少一点 / 不相关”四类明确结果反馈；反馈、完成状态与分类调整跨运行端原子保存
- “少一点”只把同类事件降为摘要，“不相关”只收入中心；价格条件保持用户设置的高优先级，所有事件仍可追溯
- 提醒新增有效期，过期事件不再打断或占用未读计数；盘中异动 2 小时、阶段/价格 24 小时、主动日程按场景设置 4-36 小时
- “深脉学到了什么”完整展示当前降噪调整，可逐类恢复、恢复全部或清除学习记录；不使用停留时间、浏览轨迹等隐式画像
- Harness 白名单上下文只接收明确反馈摘要；DeepSeek 可解释但不得修改授权、推断未记录偏好或触发交易
- 版本升级至 1.10.0，四端增加 attention-learning capability 门禁

### R29 · 事件影响路径与跨端主动雷达 ✅ 已完成
- 总览新增默认关闭的“事件影响雷达”；用户单独授权后才访问 AKShare 宏观日历与市场快讯，宏观/快讯/自选关联范围可分别控制并随时停止
- 新增 `event-impact-v1` 四层契约：事件事实（来源、计划/观测时点、实际/预期/前值）、透明敏感性规则、字段质量与可选 AI 解释；不把相关性包装成因果或方向预测
- AKShare `macro_info_ws` 与 `news_economic_baidu` 双源归一和互证；当前本机旧版接口存在时直接兼容，单源失败时明确降级，不阻断行情主链路
- 全 A 股票目录补充行业字段；事件可通过行业、分组、备注或直接提及命中用户自选，并展示每一步的匹配依据
- 新增本机后台事件服务、原子去重回执和统一提醒中心送达；页面关闭后继续运行，关闭本机服务即停止，提醒学习规则继续生效
- Harness 增加严格白名单事件上下文和非因果提示；墨水屏新增 `event` 模式，共用同一事件摘要、质量和自选命中状态
- 版本升级至 1.11.0，四端增加 `event-impact-v1` 与 `event-background-service-v1` 能力门禁

### R30 · 可反证研究假设闭环 ✅ 已完成
- 事件卡片新增 1/3/5/10/20 个工作日观察窗口和“保存研究假设”；同一事件的未完成假设原子去重，不会因多端点击重复创建
- `research-hypothesis-v1` 保存创建时事件来源、观测时点、质量、行业路径、自选命中、观察清单和三类预设反证；明确禁止因果、方向预测与自动交易声明
- 策略页新增研究假设中心，区分观察中、待复盘、已完成与归档；最终结论仅允许用户确认“支持/混合/不支持/事件失效”
- 本机后台按预登记到期时间发布一次复盘提醒，不访问任何新外部来源、不扩大事件雷达授权；提醒仍进入统一中心并接受显式降噪反馈
- Harness 增加严格白名单假设上下文和反事后偏差提示；长上下文压缩优先保留原始假设、反证与用户结论
- 版本升级至 1.12.0，四端增加 `research-hypothesis-v1` 与 `hypothesis-due-reminders-v1` 能力门禁

### R31 · 候选证据与市场对照时间线 ✅ 已完成
- 首次收集冻结标的与上证指数点时基线，保留来源和精确采集时点，并明确不保证是收盘价，禁止后来价格倒填创建时事实
- 观察期内按日期幂等整理巨潮资讯官方公告、标的累计变化、同期市场变化和相对差值；每条证据保留可知时间、观察时间、来源层级与非因果解释
- `hypothesis-evidence-v1` 只生成候选证据，不自动判断支持/不支持；用户复盘结论、原假设与预设反证均不会被采集任务改写
- 已授权事件服务时，本机后台每 15 分钟最多检查一次观察中假设；未授权时不自动访问外部来源，策略页仍提供明确的手动收集动作
- 策略页新增对照基线和候选证据时间线；Harness 严格清洗基线、来源、事实和指标，并把“相对表现不等于事件因果”写入模型边界
- 版本升级至 1.13.0，四端增加 `hypothesis-evidence-candidates-v1` 与 `hypothesis-market-control-v1` 能力门禁

### R32 · 统一跨端送达回执与系统级通知 ✅ 已完成
- 工作台提醒中心、Windows 系统通知与墨水屏复用同一事项 ID；每个已授权终端分别记录认领、成功或失败回执，避免同端重复打扰
- 提醒偏好增加 Windows 与墨水屏独立开关，默认均关闭；首次开启只接收授权后的新提醒，避免历史积压突然补发
- 桌面 App 本机轮询统一投递接口，通知点击后回到深脉窗口；安静时段、暂停、重要性和显式学习降噪规则保持一致
- ESP32 帧协议增加投递事项头，只有真实屏幕刷新成功才回传送达；传输或刷新失败时租约到期后可安全重试
- 提醒中心展示两类终端的开关与累计送达状态；投递行为不扩大事件、行情或硬件网关原有授权
- 版本升级至 1.14.0，四端增加 `unified-delivery-v1`、`desktop-system-notifications-v1` 与 `epaper-delivery-receipts-v1` 能力门禁

### R33 · 通知精确落点与送达时间线 ✅ 已完成
- Windows 通知保留事项 ID 和目标页面；用户点击后自动切入 Harness 的深脉一级视图、打开提醒中心并高亮对应事项
- 深链只接受白名单页面和最多 160 字符的事项 ID，不允许通知构造任意网页或执行入口
- 每张提醒卡片直接展示 Windows、墨水屏的发送中、已送达或失败状态、送达时间和重试次数；“送达”继续与“已读/已处理”严格分离
- 失败回执展示可读原因和手动重试按钮；只允许失败项重新排队，已成功送达事项不会因误触再次打扰
- 回执补充提醒标题、分类和页面快照，即使提醒中心后续淘汰旧事项，最近投递记录仍保留可解释语义
- 版本升级至 1.15.0，四端增加 `notification-deep-links-v1` 与 `delivery-timeline-v1` 能力门禁

### R34 · 产品级诊断与可恢复性 ✅ 已完成
- 增加一键本机诊断导出，帮助普通用户判断桌面、数据服务、Harness、网关和硬件分别卡在哪里
- 对数据源中断、桌面通知权限受限、设备长时间离线提供分层修复建议，不再只显示技术错误
- 在不上传隐私和密钥的前提下生成可分享诊断包，为远程协助和开源社区问题反馈建立标准材料
- Windows 桌面 App 每 30 秒发送本机心跳，诊断页能区分“浏览器可用”与“系统提醒需要桌面 App”，避免把可选能力误报成故障
- 诊断报告使用字段白名单重新组装，明确排除 API 密钥、设备令牌、本机路径、IP、自选股、提醒正文与聊天记录
- 版本升级至 1.16.0，四端增加 `product-diagnostics-v1`、`diagnostics-export-v1` 与 `desktop-heartbeat-v1` 能力门禁

### R35 · 可执行修复与服务韧性 ✅ 已完成
- 为可安全自动处理的问题增加“一键修复”，高风险动作保持人工确认
- 保存最近诊断基线，帮助用户看清偶发中断、持续故障与已恢复状态
- 为开源 Issue 生成结构化模板，降低远程协助沟通成本
- 自动动作严格限制为只读行情复核、通达信只读探测、AKShare 日历核对，以及重启用户已经开启的墨水屏网关；不会代替用户授权、启动外部程序或重新发送提醒
- 新增公开行情主源与腾讯备援的组合诊断，区分“尚未观测、主源降级、双源中断”，避免把未使用误报为故障
- 诊断基线只保存组件 ID、状态和时间，状态不变时 30 分钟内去重，最多保留 96 条；界面显示刚出现、持续存在和已恢复
- 脱敏诊断包新增 `github-issue.md`，可直接作为公开仓库的问题反馈模板
- 版本升级至 1.17.0，四端增加 `diagnostic-repairs-v1`、`diagnostic-history-v1` 与 `diagnostic-issue-template-v1` 能力门禁

### R36 · 个性化工作节奏与主动服务编排 ✅ 已完成
- 用户可用自然语言描述盘前、盘中、盘后、重要性和安静时段；本机规则解析只生成透明草稿，用户明确确认后才原子保存
- 草稿逐条展示已理解内容、未决事项、置信度与实际变化；提到关注对象不会暗中修改自选股
- 主动日程增加未来服务时间线，明确区分待执行、已完成、已跳过、已错过和暂停中
- 支持一次性跳过下一次服务、暂停到次日早晨和随时恢复；所有动作可逆，不连接交易账户
- 版本升级至 1.18.0，四端增加 `service-plan-preview-v1`、`service-plan-confirm-v1`、`routine-timeline-v1` 与 `routine-skip-pause-v1` 能力门禁

### R37 · 主动服务效果闭环 ✅ 已完成
- 以“是否帮助用户完成研究任务”为核心衡量效果，盘前、盘中、盘后分别展示生成、反馈、帮助、完成与负向次数
- 反馈只来自用户明确选择的“有用、已完成、少一点、不相关”；未反馈、打开率、停留时间和浏览轨迹不进入效果判断
- 同一时段至少 3 次明确反馈、负向至少 2 次且多于正向时才生成关闭建议，不把小样本波动包装成个性化结论
- 节奏建议默认只展示证据，用户明确确认后才关闭对应时段；操作留有原因和记录，可一键撤销恢复
- 版本升级至 1.19.0，四端增加 `routine-effectiveness-v1`、`routine-effect-suggestions-v1` 与 `routine-effect-undo-v1` 能力门禁

### R38 · 个人研究驾驶舱 ✅ 已完成
- 把自选、事件假设、复盘任务、服务效果和数据健康收拢为一张个人研究地图
- 让用户每天快速看清“现在最值得研究什么、为什么、证据是否齐全、下一步做什么”
- 所有优先级均可解释、可调整，不把模型推断伪装成用户目标或市场事实
- 支持置顶、提高、降低、明天再看和恢复默认；队列控制与原始研究记录隔离，所有操作可逆
- 版本升级至 1.20.0，四端增加 `research-cockpit-v1`、`research-priority-controls-v1` 与 `research-cockpit-context-v1` 能力门禁

### 下一阶段：研究记忆与复盘成长
- 把用户明确确认的研究结论、反证命中和数据缺口形成可回看的研究模式，而不是交易胜率标签
- 在新任务出现时提示“过去是否遇到过相似研究结构”，只引用用户自己的已确认记录并允许关闭
- 让 DeepSeek 帮助总结研究方法的改进点，但不根据收益结果倒推因果或自动改变策略

### R39 · 研究记忆与复盘成长 ✅ 已完成
- 只有用户明确确认的假设复盘才进入研究记忆；结论、反证命中、数据缺口与可知时间均可追溯，不从收益或隐式行为生成“经验”
- 新假设按事件类型、产业与自选重合度提示相似历史研究结构；相似提示不参与优先分、不继承历史结论，可整体关闭
- 每条记忆支持用户补充方法改进、隐藏和恢复；所有偏好与原始假设隔离，不会改写研究证据或复盘结论
- Harness 只接收白名单化的已确认记忆；DeepSeek 可帮助总结方法，但不得统计交易胜率、倒推因果、自动保存结论、修改策略或触发交易
- 修复新建研究假设中遗留的乱码模板，后续生成的观察清单、反证条件和边界均使用可读中文
- 版本升级至 1.21.0，四端增加 `research-memory-v1`、`research-memory-controls-v1` 与 `research-memory-context-v1` 能力门禁

### R40 · AKShare 研究增强快照 ✅ 已完成
- 数据源页增加按需研究增强快照，首批覆盖 PMI、CPI、PPI、LPR 与中美国债收益率；默认不请求，生成后缓存 6 小时
- 每项指标保留数据日期、陈旧天数、时效状态、AKShare 接口与最终上游；未来空值不倒填，失效接口逐项降级
- 相同最终上游使用同一 `independentGroup`，明确禁止把 AKShare 包装的东方财富与直连东方财富统计为独立互证
- 市场宽度继续使用现有东方财富主链和通达信只读复核，不为扩大“数据源数量”重复接入不稳定包装接口
- 研究增强不进入情绪评分、仓位区间、提醒或交易；Harness 白名单保留时效和来源血缘，并要求模型先核对再解释
- 版本升级至 1.22.0，四端增加 `akshare-research-snapshot-v1` 与 `source-lineage-v1` 能力门禁

### R41 · 桌面服务生命周期保护 ✅ 已完成
- 桌面 App 创建 Windows Job Object，并对自己启动的 DeepPulse 与 Harness 子进程启用 `KILL_ON_JOB_CLOSE`
- 正常关闭继续执行有序回收；任务管理器结束、更新替换或外壳崩溃时由 Windows 内核兜底清理，避免旧服务占用 8971/8988 或继续提供旧资源
- 只托管 App 自己创建的进程；对用户自行启动、桌面仅附着使用的兼容服务不获取所有权、不随 App 关闭
- 生命周期保护初始化或进程加入失败时写入本地桌面日志并回退到原有有序关闭，不把可选保护失败包装成启动失败
- 版本升级至 1.22.1，公开清单增加 `desktop-process-lifetime-v1`

### R42 · 用户可控研究流程与自由编排 ✅ 已完成
- 用户可把“研究对象、问题、证据源、复盘时点和输出方式”组合成可预览、可暂停、可恢复、可完成和可复制的个人研究流程
- 支持一次性研究任务与可复用模板；创建前展示确定步骤、环境状态和逐项权限，所有外部来源与本机到期提醒均需明确确认
- 运行时只读取已经冻结并获准的来源，官方披露、公开行情、通达信、AKShare 和事件快讯逐项隔离失败并保留来源时点
- AKShare 作为宏观与跨市场研究增强层接入，保留最终上游，不替代官方披露与行情主链路，也不进入情绪评分或交易触发
- Harness 只接收有边界的流程摘要与最近运行证据；DeepSeek 负责解释、拆解和协作建议，不能修改权限、提醒、流程状态或代替用户执行
- 版本升级至 1.23.0，四端增加 `research-workflows-v1`、`research-workflow-preview-v1` 与 `research-workflow-permissions-v1` 能力门禁

### R43 · 研究结果卡片与可追溯复盘闭环 ✅ 已完成
- 最近一次流程运行结果生成可阅读的事实卡，明确展示来源、候选证据、缺口、陈旧项与相同最终上游
- 结果卡可由用户填入复盘日历草稿，但不会自动保存、自动判断研究问题成立或自动生成交易动作
- Harness 只接收白名单后的结果卡事实；墨水屏新增只读取本机已保存结果的“研究结果”模式
- 版本升级至 1.24.0，四端增加 `research-result-cards-v1` 与 `epaper-research-workflow-v1` 能力门禁

### R44 · 参数化研究模板与运行对比 ✅ 已完成
- 模板保存研究方法、来源组合、输出方式与复盘周期；套用时原标的被替换为显式参数，必须重新填写并重新预览权限
- 模板不继承旧运行、旧证据、旧结果卡或旧结论，避免跨标的复制判断
- 同一流程的相邻两次运行生成事实差异，只比较来源可用性、证据数、陈旧项、数据缺口与同源组变化
- Harness 只接收白名单化模板边界和运行差异；DeepSeek 不得把采集变化解释为方向结论或交易动作
- 版本升级至 1.25.0，四端增加 `research-template-parameters-v1` 与 `research-run-comparison-v1` 能力门禁

### R45 · 研究流程版本与证据时间轴 ✅ 已完成
- 每个新流程保存服务端生成的方法族、版本号与来源流程；复制和模板实例化不能由前端伪造历史来源
- 版本摘要逐项说明研究对象、问题、来源、输出、复盘周期与提醒的变化，旧方法和旧运行继续只读保留
- 证据时间轴从已保存运行派生，分别保留观察时间、数据时点、来源状态、最终上游与同源组，新运行不覆盖旧证据
- Harness 只接收白名单化版本与时间轴；DeepSeek 只能解释方法和可知时间，不得把版本或时间变化包装成方向结论
- 版本升级至 1.26.0，四端增加 `research-workflow-lineage-v1` 与 `research-evidence-timeline-v1` 能力门禁

### R46 · 用户可控 AKShare 研究数据包 ✅ 已完成
- 将固定宏观快照升级为增长、价格、流动性、利率和储备五类可选数据包；选择保存在本机，联网读取仍需用户点击
- 替换已陈旧的年度接口，新增 GDP、M1/M2、SHIBOR、外储和黄金储备，并按月度、季度或日频分别判定时效
- 每项保留 AKShare 接口、最终上游与独立来源组；快照增加接口耗时、失败次数和最近成功时间，接口可调用不再等同于数据新鲜
- DeepSeek 只接收用户已选择数据包的白名单指标和接口健康；这些背景不进入情绪评分、提醒或交易触发
- 版本升级至 1.29.0，四端增加 `akshare-research-packs-v1` 与 `akshare-interface-health-v1` 能力门禁

### 下一阶段：主动研究建议的可控收件箱
- 基于用户已授权的关注范围、到期复盘和明确数据缺口生成少量研究建议；建议默认只进入收件箱，必须由用户确认后才能创建流程或访问新来源
- 1.29.0：增加可控的“主动研究建议收件箱”。建议只来自用户自选、已保存假设和明确数据缺口；载入仅复制为可编辑草稿，必须继续经过实时预览、逐项来源授权和确认创建，且创建后仍不自动运行。忽略、恢复、过期和成功转为流程均保留本机状态，DeepSeek 只能解释依据，不能代替用户操作。

### R47 · 主动研究建议的可控收件箱 ✅ 已完成
- 基于明确自选、到期复盘和已记录数据缺口，用本地确定性规则生成少量研究建议；默认只进入策略页收件箱
- 载入建议只复制为可编辑草稿并备份旧草稿，不会自动预览、授权、创建或运行；仅当实时预览指纹一致且工作流创建成功后才记为已采纳
- 忽略、恢复、过期和已采纳均保留本机状态；DeepSeek 只接收白名单依据与安全草稿
- 版本保持 1.29.0，四端增加 `research-suggestions-v1` 与 `research-suggestion-preview-v1` 能力门禁

### R48 · 可信注意力分诊与热点关联降噪 ✅ 已完成
- 相同关注标的、透明主题和北京时间日期的事件提醒合并为可展开主题，未读角标按可处理主题计数
- 原始事件、来源、观测时点和投递记录继续保留；聚合不等于来源互证、因果证明或方向判断
- 高优先级价格条件继续独立即时提醒；整组已读、完成和反馈都需要用户明确操作
- 行业间接关联由单关键词升级为至少两个同主题关键词；直接公司名称或代码仍保持高置信关联
- 版本升级至 1.29.0，四端增加 `attention-triage-v1` 与 `event-impact-v2` 能力门禁

### v1.30 · 研究接力闭环

- 将首页“尚无研究假设”与策略页主动建议合并为同一条研究事项，避免跨页面重复提醒。
- 用户点击后才载入服务器保存的可编辑草稿；载入、预览、创建、运行分别记录为明确阶段，任何阶段都不会自动进入下一步。
- 已创建的研究流程继续回到首页研究驾驶舱，展示“检查后手动运行 / 查看结果 / 到期复盘”等真实下一步，不再创建后消失。
- 忽略建议会同步从首页移除；编辑后的草稿转为独立研究，不会错误关闭或推进原建议。
- AKShare 继续作为按需、可选的研究增强层，只在用户明确选择并手动运行流程时读取，不进入情绪温度或交易触发。
- 版本升级至 1.30.0，四端增加 `research_handoff-v1` 与 `research_journey-v1` 能力门禁。

### v1.31 · 主动服务硬边界

- 修复单条事项 `center_only` 在前端被降为摘要、等待摘要周期后仍可能进入 Windows 或墨水屏投递队列的问题。
- 前端创建事项时完整保留“立即 / 摘要 / 只收入中心”三态；本地弹出判断对单条 `center_only` 明确返回不打断。
- 服务端在全局模式、分类学习和优先级判断之外增加事项级硬隔离；即使是高优先级价格条件，只要该事项明确要求留在中心就永不外发。
- 投递状态新增“只留中心”数量与原因汇总，用户能直接核验系统是否遵守设置；原事项仍保留在提醒中心，不丢失、不自动完成。
- 版本升级至 1.31.0，四端增加 `attention-center-only-boundary-v1` 能力门禁。

### v1.32 · AI 回答新鲜度与历史隔离

- 真实运行审计发现：当前工作台已是 52° 发酵期时，助手仍无时点展示旧的 43° 修复期回答，历史结论容易被误认成当前事实。
- 市场相关回答新增生成时间、行情数据日、当时温度与阶段快照；数据状态变化或超过 15 分钟后明确标记为“历史回答”。
- 历史卡同时显示当时与当前市场状态，并提供“基于当前数据重新回答”；刷新仍由用户点击，不自动联网、不覆盖旧回答。
- 对旧版本缺失时点的回答显示“旧版本未记录数据时点”，不伪造生成时间；导航确认等非市场回答保持轻量展示。
- 下一次请求发给云端大脑前会排除已失效助手文本，但保留用户原问题，防止旧模型结论污染当前上下文。
- 版本升级至 1.32.0，四端增加 `chat-answer-freshness-v1` 能力门禁。

### v1.33 · 用户逐流程授权的研究值守

- 已创建的一次性研究流程可逐条开启值守；开启前冻结原问题、原来源、每日或收盘后频率、结束时间与提醒范围，并要求逐项确认持续访问权限。
- 后台只重复读取该流程已确认来源；首次检查只建立基线，普通价格变化不触发提醒，新公告、候选事件集合、来源降级/恢复或时效变化才生成去重事项。
- 默认只进入提醒中心；Windows 与墨水屏仍由用户原有终端授权控制。没有变化只更新最近检查，不制造“AI 很忙”的噪声。
- 用户可立即检查、暂停、恢复或结束；授权最长 31 天，到期、流程暂停/完成或方法指纹变化后停止读取并要求重新授权。
- 连续三次失败的来源自动暂停并只生成一条修复事项；其他来源继续隔离运行。DeepSeek 只在用户点击后解释已保存变化，不能开启值守、扩权、下结论或触发交易。
- 版本升级至 1.33.0，四端增加 `research-watch-v1` 能力门禁。

### v1.34 · 主动服务管理中心

- 将总览中的主动服务日程、自然语言配置、服务时间线、效果学习和事件雷达授权移入独立管理中心，减少低频配置对首屏市场信息的挤压。
- 总览保留紧凑状态条，统一展示开启项目数量与名称、下一次服务、暂停、降级和异常；异常时提供明确检查入口。
- 管理中心沿用原有逐项授权与确认流程，打开页面不改变配置、不访问新来源，也不自动应用自然语言草稿。
- 桌面端提供受限高度的滚动对话框，移动端提供全屏界面；支持原生 Escape 关闭、背景关闭与焦点返回。
- 新增纯状态模型与覆盖未授权、组合开启、暂停、来源降级和陈旧关闭状态的回归测试。
- 版本升级至 1.34.0，四端增加 `service-management-center-v1` 能力门禁。

### v1.35 · 可确认的 AI 行动单

- DeepSeek 与本地智脑只能提出行动意图；涉及自选或情绪历史的写操作由服务端转换为待确认行动单，回复生成阶段不再修改档案。
- 行动单明确展示目标、影响范围、是否可撤销与非交易边界；用户确认后，服务端按 `planId`、行动指纹和当前相关状态重新校验。
- 加入/移出自选支持 30 分钟撤销，撤销会保留原分组、备注和顺序；自选在期间发生其他变化时拒绝覆盖。
- 情绪快照只在确认后记录，按数据日幂等且明确不可撤销；每次执行、跳过、失败或撤销都返回可读回执。
- 未知动作、非白名单页面、非法代码、过期或陈旧行动单全部拒绝；模型伪造的 `confirmed=true` 没有任何效力。
- 版本升级至 1.35.0，四端增加 `chat-action-plan-v1` 与 `chat-action-receipts-v1` 能力门禁。

### v1.36 · 主动事项精准落点与统一处置回执

- 每条主动事项由服务端生成受限的类型化落点，明确页面、对象、视图和状态指纹；提醒中心、研究驾驶舱与桌面通知复用同一个目标。
- 点击后必须真的找到股票、研究流程、假设、复盘日期或数据诊断对象，才记录“已打开”；定位失败会回到提醒中心，且不会制造已读或已处理假象。
- “未处理、已打开、处理中、已处理”与“未读、已读”分离；打开不等于解决，质量反馈也不再兼任任务完成按钮。
- 研究值守提醒会展开对应流程的最新变化与证据时间线、聚焦标题，并提供开始处理、标记已处理、重新处理和返回提醒中心。
- 所有处置写入都校验当前目标指纹并保存最小本机回执；过期目标、任意 URL、选择器、权限和模型自带确认均无法执行。
- 版本升级至 1.36.0，四端增加 `proactive-target-v1` 与 `disposition-receipts-v1` 能力门禁。
